name: CI

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Ruff Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "apps/core/uv.lock"
      - name: Install dependencies
        run: uv sync --frozen
        working-directory: apps/core
      - name: Run ruff
        run: uv run ruff check .
        working-directory: apps/core

  typecheck-py:
    name: MyPy Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "apps/core/uv.lock"
      - name: Install dependencies
        run: uv sync --frozen
        working-directory: apps/core
      - name: Run mypy
        run: uv run mypy app
        working-directory: apps/core

  check-types:
    name: TypeScript Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "apps/core/uv.lock"
      - name: Install Python deps for core:check-types
        run: uv sync --frozen
        working-directory: apps/core
      - name: Install JS deps
        run: bun install --frozen-lockfile
      - name: Run check-types
        run: bun run check-types

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Run build
        run: bun run build


